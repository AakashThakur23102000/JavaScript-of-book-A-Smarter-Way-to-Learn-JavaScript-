/*You can also assign a number like this  */

var weights = 150;
alert(weights);

/* add in weight */
alert(weights + 25);

/*Assigning new vairable to the sum*/
var oldweight = 40;
var newweight = oldweight + 5;
alert(newweight)

/*A variable can be used in calculating its own new value. */
var originalNum = 90;
originalNum = originalNum + 10;
alert(originalNum);

/* you can directly print the values wighout variable */
alert(155);